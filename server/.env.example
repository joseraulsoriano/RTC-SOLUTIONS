# Copia este archivo a .env y ajusta las claves que tengas
# Puerto del servidor
PORT=8080

# Proveedor de búsqueda (opcional). Si existe BRAVE_API_KEY, se usará Brave.
# Si no, si existe BING_API_KEY, se usará Bing API.
# Si ninguno existe, se usarán scrapers (DuckDuckGo/Bing HTML) como fallback.
BRAVE_API_KEY=
BING_API_KEY=